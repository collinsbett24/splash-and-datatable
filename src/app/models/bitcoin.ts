export interface Bitcoin {
    id: string,
    symbol: string,
    name: string,
    image: string,
    market_cap: number,
    market_cap_rank: number,
    current_price: number,
    last_updated: Date
}
